package login;

//import java.io.IOException;

//import org.openqa.selenium.WebDriver;
//import org.openqa.selenium.firefox.FirefoxDriver;
//import org.openqa.selenium.remote.CapabilityType;
//import org.openqa.selenium.remote.DesiredCapabilities;
//import org.testng.annotations.BeforeTest;
//import org.testng.annotations.Test;

public class Proxy {
	
//@BeforeTest
//	public void setUpDriver(){
//	FirefoxProfile profile = new FirefoxProfile();
//	profile = FirefoxProfile()
//	profile.setPreference("network.proxy.type", 1);
//	profile.setPreference("network.proxy.http", 46.102.106.59);
//	profile.setpreference("network.proxy.http_port" 13228);
//	profile.setpreference("network.proxy.ssl", 46.102.106.59);
//	profile.setpreference("network.proxy.ssl_port", 13228');
//	browser = webdriver.Firefox(profile);

//	public static void main(String[] args) {
//		// TODO Auto-generated method stub
//		
//	}
	//WebDriver driver;

	 /*@BeforeTest
	 public void setUpDriver() {
	  //Set proxy IP and port. Here localhost Is proxy IP and 8080 Is Port number.
	  //You can change both values as per your requirement.
	  String PROXY = "localhost:8080";
	  //Bellow given syntaxes will set browser proxy settings using DesiredCapabilities.
	  Proxy proxy = new Proxy();
	 setHttpProxy(PROXY).setFtpProxy(PROXY).setSslProxy(PROXY).setSocksProxy(PROXY);
	  DesiredCapabilities cap = new DesiredCapabilities();
	  cap.setCapability(CapabilityType.PROXY, proxy);
	  //Use Capabilities when launch browser driver Instance.
	  driver = new FirefoxDriver(cap);
	 }


	@Test
	 public void start() throws IOException {
	  System.out.println("Check your webdriver driver Instance's proxy setttings.");
	 }*/
}
